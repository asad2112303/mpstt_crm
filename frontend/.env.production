# Production build defaults — URLs only, no key material (the secret scanner
# correctly rejects key-shaped strings in git, even publishable ones).
#
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY is supplied by the hosting platform's
# environment variables (Vercel → Settings → Environment Variables).
NEXT_PUBLIC_SUPABASE_URL=https://rhghnfmilvetlxmcbkjl.supabase.co
NEXT_PUBLIC_API_BASE_URL=https://mpstt-crm-api-production.up.railway.app
